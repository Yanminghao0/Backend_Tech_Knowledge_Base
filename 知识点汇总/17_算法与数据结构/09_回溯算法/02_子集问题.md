# 子集问题

> 子集问题是回溯算法的经典应用，用于找出一个集合中所有可能的子集组合，包括空集和集合本身。根据输入集合是否包含重复元素，可分为基本子集问题和含重复元素的子集问题。

---

## 📋 基本信息
- **类别**：回溯算法、组合数学
- **难度**：中等
- **核心思想**：通过递归构建子集，控制元素的选择状态
- **时间复杂度**：O(2ⁿ)（n为元素个数）
- **空间复杂度**：O(n)（递归栈深度）
- **关键词**：回溯、子集生成、递归、剪枝、去重

## 🎯 问题分析
子集问题主要分为以下两类：
1. **无重复元素的子集**：输入集合中的元素互不相同，找出所有可能的子集
2. **有重复元素的子集**：输入集合中包含重复元素，找出所有不重复的子集

子集问题的本质是：
- 每个元素有两种状态：被选中或不被选中
- 通过递归遍历所有元素的选择状态
- 收集所有可能的选择组合

解决子集问题的通用思路：
1. **递归回溯**：使用递归探索每个元素的选择可能性
2. **路径记录**：维护当前子集状态（路径）
3. **结果收集**：在递归的每一步收集当前路径作为一个子集
4. **剪枝优化**：对重复元素进行排序和跳过处理

## 💻 代码实现
### Java实现（子集问题经典解法）
#### 1. 无重复元素的子集
```java
import java.util.*;

public class Subsets {
    public List<List<Integer>> subsets(int[] nums) {
        List<List<Integer>> result = new ArrayList<>();
        if (nums == null) return result;
        backtrack(nums, 0, new ArrayList<>(), result);
        return result;
    }

    // 回溯函数：从start位置开始选择元素
    private void backtrack(int[] nums, int start, List<Integer> path, List<List<Integer>> result) {
        // 收集当前路径作为一个子集
        result.add(new ArrayList<>(path));

        // 遍历选择列表：从start开始，避免重复子集
        for (int i = start; i < nums.length; i++) {
            // 做选择
            path.add(nums[i]);
            // 递归探索：下一个元素从i+1开始
            backtrack(nums, i + 1, path, result);
            // 撤销选择
            path.remove(path.size() - 1);
        }
    }

    public static void main(String[] args) {
        Subsets solution = new Subsets();
        int[] nums = {1, 2, 3};
        List<List<Integer>> result = solution.subsets(nums);
        System.out.println("所有子集：");
        for (List<Integer> subset : result) {
            System.out.println(subset);
        }
    }
}
```

#### 2. 有重复元素的子集（去重实现）
```java
import java.util.*;

public class SubsetsWithDup {
    public List<List<Integer>> subsetsWithDup(int[] nums) {
        List<List<Integer>> result = new ArrayList<>();
        if (nums == null) return result;
        // 排序，使重复元素相邻
        Arrays.sort(nums);
        backtrack(nums, 0, new ArrayList<>(), result);
        return result;
    }

    private void backtrack(int[] nums, int start, List<Integer> path, List<List<Integer>> result) {
        // 收集当前路径作为一个子集
        result.add(new ArrayList<>(path));

        for (int i = start; i < nums.length; i++) {
            // 去重剪枝：跳过重复元素
            if (i > start && nums[i] == nums[i - 1]) {
                continue;
            }

            // 做选择
            path.add(nums[i]);
            // 递归探索
            backtrack(nums, i + 1, path, result);
            // 撤销选择
            path.remove(path.size() - 1);
        }
    }

    public static void main(String[] args) {
        SubsetsWithDup solution = new SubsetsWithDup();
        int[] nums = {1, 2, 2};
        List<List<Integer>> result = solution.subsetsWithDup(nums);
        System.out.println("去重子集：");
        for (List<Integer> subset : result) {
            System.out.println(subset);
        }
    }
}
```

## 📊 剪枝策略
子集问题的剪枝主要用于处理重复元素，核心思路是：

### 1. 排序去重法
- **步骤**：
  1. 对数组进行排序，使重复元素相邻
  2. 当遇到重复元素时，只允许第一个未被使用的元素被选择
  3. 跳过后续相同元素，直到前一个相同元素被使用
- **代码实现**：
  ```java
  if (i > start && nums[i] == nums[i - 1]) {
      continue; // 跳过重复元素
  }
  ```
- **效果**：避免生成重复子集，将时间复杂度从O(2ⁿ×n)优化为O(2ⁿ)

### 2. 二进制枚举法（非回溯）
- **思路**：使用位运算枚举所有可能的子集
- **适用场景**：元素数量较少（通常n≤20）的情况
- **代码示例**：
  ```java
  public List<List<Integer>> subsetsByBit(int[] nums) {
      List<List<Integer>> result = new ArrayList<>();
      int n = nums.length;
      // 枚举所有2ⁿ种可能
      for (int mask = 0; mask < (1 << n); mask++) {
          List<Integer> subset = new ArrayList<>();
          for (int i = 0; i < n; i++) {
              // 检查第i位是否为1
              if ((mask & (1 << i)) != 0) {
                  subset.add(nums[i]);
              }
          }
          result.add(subset);
      }
      return result;
  }
  ```

## 🔍 经典题目
| 题目 | 难度 | 考点 | 链接 |
|------|------|------|------|
| 子集 | 中等 | 基本子集生成 | [LeetCode 78](https://leetcode-cn.com/problems/subsets/) |
| 子集 II | 中等 | 含重复元素的子集 | [LeetCode 90](https://leetcode-cn.com/problems/subsets-ii/) |
| 递增子序列 | 中等 | 非排序去重子集 | [LeetCode 491](https://leetcode-cn.com/problems/increasing-subsequences/) |
| 字母大小写全排列 | 简单 | 字符子集 | [LeetCode 784](https://leetcode-cn.com/problems/letter-case-permutation/) |
| 拆分成最多数目的正偶数之和 | 中等 | 特殊子集和 | [LeetCode 2178](https://leetcode-cn.com/problems/maximum-split-of-positive-even-integers/) |

## ⚠️ 注意事项
1. **重复元素处理**：必须先排序再去重，否则剪枝逻辑无法生效
2. **递归终止条件**：子集问题不需要显式终止条件，遍历完所有元素自然结束
3. **结果收集时机**：在递归的每一层都收集当前路径，而非仅在叶子节点
4. **起始索引控制**：通过start参数控制元素选择范围，避免重复子集
5. **空集处理**：回溯算法从空集开始，自然包含空集结果
6. **元素顺序**：子集问题不考虑元素顺序，{1,2}与{2,1}视为同一子集
7. **数据规模限制**：回溯法在n>20时可能超时，需考虑非回溯解法

## 🎓 最佳实践
- **理解子集生成过程**：画出递归树，直观理解子集生成的过程
- **掌握去重技巧**：熟练运用排序+跳过重复元素的剪枝方法
- **多种解法对比**：比较回溯法与二进制枚举法的适用场景
- **路径复用**：使用同一个列表对象进行回溯，避免频繁创建新对象
- **非递归实现**：了解迭代法实现子集生成，拓宽解题思路
- **组合问题转化**：将组合问题转化为子集问题求解
- **复杂度分析**：明确子集问题的时间复杂度为O(2ⁿ)，空间复杂度为O(n)

---