# 斐波那契查找

> 斐波那契查找是一种基于黄金分割原理的有序查找算法，通过斐波那契数列确定分割点，具有平均性能优于二分查找的特点

---

## 📋 基本信息

- **类型**: 有序查找算法
- **时间复杂度**: O(log n)
- **空间复杂度**: O(1)
- **适用条件**: 
  - ✅ 有序数据集合
  - ✅ 随机访问数据结构（数组）
  - ✅ 数据元素分布均匀或呈现一定规律
- **核心思想**: 利用斐波那契数列特性确定查找分割点，使查找区间按黄金比例分割

---

## 🎯 算法原理

### 1. 基本思路
1. 找到最小的斐波那契数F[k]，使得F[k]-1 ≥ 数组长度n
2. 将原始数组扩展至长度F[k]-1（不足部分用最后一个元素填充）
3. 设置初始指针：low=0，high=F[k]-2，mid=low+F[k-1]-1
4. 比较数组中mid位置元素与目标值target
5. 若相等，则查找成功，返回mid（需调整至原始数组范围内）
6. 若arr[mid] > target，则在左侧子区间查找，更新high=mid-1，k=k-1
7. 若arr[mid] < target，则在右侧子区间查找，更新low=mid+1，k=k-2
8. 重复步骤3-7，直到找到目标或查找区间为空

### 2. 核心流程图
```mermaid
graph TD
    A[开始] --> B[计算最小斐波那契数F[k] ≥ n+1]
    B --> C[扩展数组至长度F[k]-1]
    C --> D[初始化 low=0, high=F[k]-2, k=k]
    D --> E{low ≤ high?}
    E -- 是 --> F[计算 mid=low+F[k-1]-1]
    F --> G{mid ≥ n?}
    G -- 是 --> H[设置比较值为最后一个元素, flag=true]
    G -- 否 --> I[设置比较值为arr[mid], flag=false]
    H --> J{比较值 == target?}
    I --> J
    J -- 是 --> K{flag == true?}
    K -- 是 --> L[检查原始数组最后一个元素是否等于target]
    L -- 是 --> M[返回n-1, 查找成功]
    L -- 否 --> N[在左侧子区间查找, high=mid-1, k=k-1]
    K -- 否 --> O[返回mid, 查找成功]
    J -- 否 --> P{比较值 > target?}
    P -- 是 --> N
    P -- 否 --> Q[在右侧子区间查找, low=mid+1, k=k-2]
    Q --> E
    E -- 否 --> R[返回-1, 查找失败]
    M --> S[结束]
    O --> S
    R --> S
```

### 3. 斐波那契数列特性
斐波那契查找利用了斐波那契数列的以下特性：
- 斐波那契数列定义：F[0]=0, F[1]=1, F[k]=F[k-1]+F[k-2]
- 黄金分割比例：随着k增大，F[k-1]/F[k]趋近于(√5-1)/2≈0.618
- 分割点计算：mid=low+F[k-1]-1，将区间分为比例约0.618和0.382的两部分
- 区间长度关系：右侧子区间长度=F[k-2]-1，左侧子区间长度=F[k-1]-1

### 4. 与二分查找的区别
| 特性 | 二分查找 | 斐波那契查找 |
|------|----------|--------------|
| 分割比例 | 1:1 | 约0.618:0.382（黄金分割） |
| 中间位置计算 | mid=(low+high)/2 | mid=low+F[k-1]-1 |
| 比较次数 | 均匀 | 左侧子区间比较次数多 |
| 运算类型 | 除法（或右移） | 加减法 |
| 平均性能 | 较好 | 优于二分查找 |
| 最坏性能 | O(log n) | O(log n) |

---

## 💻 代码实现

### 1. 基础实现
```java
import java.util.Arrays;

/**
 * 斐波那契查找算法实现
 * @param arr 有序数组
 * @param target 目标值
 * @return 目标索引，未找到返回-1
 */
public int fibonacciSearch(int[] arr, int target) {
    if (arr == null || arr.length == 0) {
        return -1;
    }
    
    int n = arr.length;
    int low = 0;
    int high = n - 1;
    
    // 1. 计算斐波那契数列，找到最小的F[k] ≥ n+1
    int k = 0;
    int[] fib = getFibonacciArray(20); // 获取斐波那契数列
    while (fib[k] - 1 < n) {
        k++;
    }
    
    // 2. 创建扩展数组
    int[] temp = Arrays.copyOf(arr, fib[k] - 1);
    for (int i = n; i < temp.length; i++) {
        temp[i] = arr[high]; // 用原数组最后一个元素填充
    }
    
    // 3. 斐波那契查找过程
    while (low <= high) {
        int mid = low + fib[k - 1] - 1; // 计算mid位置
        
        if (mid >= n) {
            // 扩展部分，实际值等于原数组最后一个元素
            if (target > arr[high]) {
                // 目标值大于最后一个元素，在右侧查找
                low = mid + 1;
                k -= 2;
            } else if (target < arr[high]) {
                // 目标值小于最后一个元素，在左侧查找
                high = mid - 1;
                k -= 1;
            } else {
                // 找到目标值
                return high;
            }
        } else {
            if (temp[mid] == target) {
                // 找到目标值
                return mid;
            } else if (temp[mid] > target) {
                // 目标值在左侧子区间
                high = mid - 1;
                k -= 1;
            } else {
                // 目标值在右侧子区间
                low = mid + 1;
                k -= 2;
            }
        }
    }
    
    // 未找到目标值
    return -1;
}

/**
 * 生成斐波那契数列
 * @param length 数列长度
 * @return 斐波那契数列数组
 */
private int[] getFibonacciArray(int length) {
    if (length <= 0) {
        return new int[0];
    }
    
    int[] fib = new int[length];
    fib[0] = 0;
    if (length > 1) {
        fib[1] = 1;
        for (int i = 2; i < length; i++) {
            fib[i] = fib[i - 1] + fib[i - 2];
        }
    }
    
    return fib;
}
```

### 2. 优化实现（避免数组拷贝）
```java
/**
 * 斐波那契查找优化版本（避免数组拷贝）
 * @param arr 有序数组
 * @param target 目标值
 * @return 目标索引，未找到返回-1
 */
public int fibonacciSearchOptimized(int[] arr, int target) {
    if (arr == null || arr.length == 0) {
        return -1;
    }
    
    int n = arr.length;
    int low = 0;
    int high = n - 1;
    int k = 0;
    
    // 获取斐波那契数列并找到合适的k值
    int[] fib = getFibonacciArray(20);
    while (fib[k] - 1 < n) {
        k++;
    }
    
    while (low <= high) {
        int mid = low + fib[k - 1] - 1;
        int midValue;
        
        if (mid >= n) {
            midValue = arr[high];
        } else {
            midValue = arr[mid];
        }
        
        if (midValue == target) {
            // 找到目标值
            return mid >= n ? high : mid;
        } else if (midValue > target) {
            // 向左查找
            high = mid - 1;
            k -= 1;
        } else {
            // 向右查找
            low = mid + 1;
            k -= 2;
        }
    }
    
    return -1;
}
```

### 3. 递归实现
```java
/**
 * 斐波那契查找递归实现
 * @param arr 有序数组
 * @param target 目标值
 * @return 目标索引，未找到返回-1
 */
public int fibonacciSearchRecursive(int[] arr, int target) {
    if (arr == null || arr.length == 0) {
        return -1;
    }
    
    int n = arr.length;
    int[] fib = getFibonacciArray(20);
    int k = 0;
    while (fib[k] - 1 < n) {
        k++;
    }
    
    return fibonacciSearchHelper(arr, target, 0, n - 1, k, fib);
}

/**
 * 斐波那契查找递归辅助函数
 */
private int fibonacciSearchHelper(int[] arr, int target, int low, int high, int k, int[] fib) {
    if (low > high) {
        return -1;
    }
    
    int mid = low + fib[k - 1] - 1;
    int midValue;
    
    if (mid >= arr.length) {
        midValue = arr[high];
    } else {
        midValue = arr[mid];
    }
    
    if (midValue == target) {
        return mid >= arr.length ? high : mid;
    } else if (midValue > target) {
        // 向左查找
        return fibonacciSearchHelper(arr, target, low, mid - 1, k - 1, fib);
    } else {
        // 向右查找
        return fibonacciSearchHelper(arr, target, mid + 1, high, k - 2, fib);
    }
}
```

---

## 📊 复杂度分析

| 情况 | 时间复杂度 | 空间复杂度 | 说明 |
|------|------------|------------|------|
| 最好情况 | O(1) | O(1) | 目标值恰好在分割点位置 |
| 平均情况 | O(log n) | O(1) | 黄金分割比例使平均性能优于二分查找 |
| 最坏情况 | O(log n) | O(1) | 目标值在区间端点 |

### 复杂度分析要点
- 时间复杂度与二分查找相同，均为O(log n)，但常数因子更小
- 平均比较次数少于二分查找，因为左侧区间（约61.8%）比较次数较少
- 空间复杂度为O(1)，仅需几个额外变量
- 扩展数组的空间复杂度为O(F[k]-1-n)，通常可忽略不计

### 与二分查找的性能对比
| 算法 | 平均比较次数 | 最坏比较次数 | 计算mid的运算 | 缓存局部性 |
|------|--------------|--------------|--------------|------------|
| 二分查找 | 1.44log₂(n+1) | log₂n+1 | 除法/右移 | 好 |
| 斐波那契查找 | 1.38logφ(n+1) | logφn+1 | 加减法 | 更好 |

> φ为黄金分割比例(约1.618)，logφn ≈ 1.44log₂n

---

## 🔍 应用场景

### 典型应用
- 大规模有序数组的查找
- 内存受限环境下的查找
- 嵌入式系统中的查找算法
- 对比较操作成本高的场景

### 实际案例
- 数据库索引查询
- 搜索引擎的关键词查找
- 电子词典的单词查找
- 股票价格数据查询系统

---

## ⚠️ 注意事项

### 1. 数据要求
- **必须有序**: 无序数据无法应用斐波那契查找
- **随机访问**: 仅适用于数组等支持随机访问的数据结构
- **扩展处理**: 需正确处理扩展部分的比较逻辑

### 2. 实现注意事项
- **斐波那契数列生成**: 需预先生成足够长度的斐波那契数列
- **数组扩展**: 扩展部分必须使用原数组最后一个元素填充
- **mid越界处理**: 当mid超出原数组范围时的比较逻辑
- **k值更新**: 向左查找k减1，向右查找k减2，必须正确更新

### 3. 优缺点分析
**优点**: 
- 平均性能优于二分查找
- 仅使用加减法计算mid，避免除法运算，硬件实现更简单
- 缓存局部性更好，左侧区间更大，缓存命中率更高
- 分割点更接近实际数据分布，适合自然数据

**缺点**: 
- 实现复杂度高于二分查找
- 最坏情况下性能略逊于二分查找
- 对数据分布有一定要求
- 需要额外处理扩展数组部分

---

## 🎓 最佳实践

### 使用建议
1. **大规模数据优先**: 数据规模越大，斐波那契查找的优势越明显
2. **比较操作昂贵时使用**: 当比较操作耗时或耗能时，优势显著
3. **硬件资源受限场景**: 在嵌入式系统等资源受限环境中表现更好
4. **结合二分查找**: 可设计混合策略，数据量小时用二分查找，大时用斐波那契查找

### 优化技巧
- **预生成斐波那契数列**: 避免每次查找都重新生成
- **避免数组拷贝**: 使用索引判断代替实际扩展数组
- **边界检查优化**: 提前处理边界情况，减少循环内判断
- **迭代优于递归**: 优先选择迭代实现，避免递归调用开销

### 与其他查找算法的选择策略
| 场景 | 推荐算法 | 原因 |
|------|----------|------|
| 小规模数据 | 顺序查找 | 实现简单， overhead小 |
| 中等规模有序数据 | 二分查找 | 实现简单平衡，性能稳定 |
| 大规模有序数据 | 斐波那契查找 | 平均性能更优，缓存友好 |
| 频繁插入删除 | 哈希查找 | O(1)平均复杂度 |
| 内存受限系统 | 斐波那契查找 | 低运算复杂度，硬件友好 |

---

> **总结**: 斐波那契查找通过黄金分割比例确定分割点，在平均情况下性能优于二分查找，尤其适合大规模有序数据和比较操作成本高的场景。虽然实现稍复杂，但其独特的分割策略使其在特定领域具有不可替代的优势。