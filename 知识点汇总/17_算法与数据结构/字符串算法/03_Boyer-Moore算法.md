# Boyer-Moore算法

> Boyer-Moore算法是一种高效的字符串匹配算法，通过从右向左比较字符并利用坏字符和好后缀规则跳过不必要的比较，在实际应用中通常比KMP算法效率更高。

---

## 📋 基本信息

### 定义
Boyer-Moore算法是由Robert S. Boyer和J Strother Moore于1977年提出的字符串匹配算法。它的独特之处在于从模式串的末尾开始比较，并使用两个启发式规则（坏字符规则和好后缀规则）来确定跳过的字符数，从而实现高效匹配。

### 核心思想
1. **从右向左比较**：与大多数字符串匹配算法从左向右比较不同，Boyer-Moore算法从模式串的右端开始比较字符
2. **坏字符规则**：当主串中的字符与模式串不匹配时，根据主串中不匹配的字符（坏字符）在模式串中的位置，决定向右移动的距离
3. **好后缀规则**：当模式串中有部分后缀与主串匹配时，利用这部分匹配信息决定向右移动的距离
4. **选择最大移动距离**：综合坏字符规则和好后缀规则计算得到的移动距离，选择其中较大的值作为实际移动距离

### 与其他算法对比
| 算法 | 预处理时间 | 匹配时间(平均) | 匹配时间(最坏) | 空间复杂度 | 特点 |
|------|------------|----------------|----------------|------------|------|
| Boyer-Moore | O(m + k) | O(n/m) | O(nm) | O(k) | 实际应用中效率最高，实现复杂 |
| KMP | O(m) | O(n) | O(n) | O(m) | 无回溯，适合单模式匹配 |
| Rabin-Karp | O(m) | O(n + m) | O(nm) | O(1) | 适合多模式匹配，哈希冲突影响性能 |
| 暴力匹配 | O(1) | O(nm) | O(nm) | O(1) | 简单但效率低 |

> 注：m为模式串长度，n为主串长度，k为字符集大小

---

## 🎯 算法原理

### 坏字符规则(Bad Character Rule)
当主串中的字符`text[i]`与模式串中的字符`pattern[j]`不匹配时：
- 如果`text[i]`不在模式串中，将模式串移动`j+1`位
- 如果`text[i]`在模式串中，将模式串移动到使模式串中最右侧的`text[i]`与主串中的`text[i]`对齐的位置
- 移动距离 = j - last_occurrence[text[i]]，其中last_occurrence是记录字符最后出现位置的数组

### 好后缀规则(Good Suffix Rule)
当模式串的后缀`pattern[j+1..m-1]`与主串匹配，但`pattern[j]`不匹配时：
1. 寻找模式串中最长的、与后缀`pattern[j+1..m-1]`匹配的前缀
2. 如果找到这样的前缀，移动距离 = j - prefix_length + 1
3. 如果找不到这样的前缀，寻找模式串中最长的、是后缀`pattern[j+1..m-1]`的后缀的子串
4. 移动距离 = m - suffix_length

### 算法步骤
1. **预处理阶段**：
   - 构建坏字符规则的最后出现位置数组
   - 构建好后缀规则的前缀函数和后缀长度数组
2. **匹配阶段**：
   - 初始化主串指针i=0，模式串指针j=m-1
   - 从右向左比较字符：
     - 如果匹配，i--，j--
     - 如果不匹配：
       - 根据坏字符规则计算移动距离d1
       - 根据好后缀规则计算移动距离d2
       - i += max(d1, d2)，j重置为m-1
   - 当j=-1时，找到匹配，记录位置并继续搜索

### 图解说明
```
主串:    a b c a b c a b c d
模式串:  a b c d (m=4)

初始状态: i=3, j=3
text[3] = a, pattern[3] = d → 不匹配

应用坏字符规则:
坏字符是a，在模式串中最后出现位置是0
移动距离d1 = 3 - 0 = 3

应用好后缀规则:
没有好后缀，移动距离d2 = 4

选择d2=4，i += 4 → i=7

新位置: i=7, j=3
text[7] = b, pattern[3] = d → 不匹配

坏字符是b，在模式串中最后出现位置是1
d1 = 3 - 1 = 2

没有好后缀，d2=4

选择d2=4，i += 4 → i=11

此时i > n-m，匹配结束，未找到匹配
```

---

## 💻 代码实现

### 基本实现（含坏字符规则）
```python
def boyer_moore_bad_char(text, pattern):
    """
    Boyer-Moore算法基本实现（仅使用坏字符规则）
    :param text: 主串
    :param pattern: 模式串
    :return: 匹配起始位置列表
    """
    n = len(text)
    m = len(pattern)
    matches = []
    
    if m == 0 or n < m:
        return matches
    
    # 构建坏字符表：记录每个字符在模式串中最后出现的位置
    bad_char = {}
    for i in range(m):
        bad_char[pattern[i]] = i
    
    i = m - 1  # 主串指针
    while i < n:
        j = m - 1  # 模式串指针
        # 从右向左比较
        while j >= 0 and text[i] == pattern[j]:
            i -= 1
            j -= 1
        
        # 如果找到匹配
        if j == -1:
            matches.append(i + 1)
            i += m + 1  # 移动到下一个可能的位置
        else:
            # 根据坏字符规则计算移动距离
            # 获取坏字符在模式串中最后出现的位置
            bc_pos = bad_char.get(text[i], -1)
            # 移动距离 = max(1, j - bc_pos)
            move = max(1, j - bc_pos)
            i += move
    
    return matches
```

### 完整实现（坏字符+好后缀规则）
```python
def boyer_moore(text, pattern):
    """
    Boyer-Moore算法完整实现（坏字符规则+好后缀规则）
    :param text: 主串
    :param pattern: 模式串
    :return: 匹配起始位置列表
    """
    n = len(text)
    m = len(pattern)
    matches = []
    
    if m == 0 or n < m:
        return matches
    
    # ---------------------- 坏字符规则预处理 ----------------------
    bad_char = {}
    for i in range(m):
        bad_char[pattern[i]] = i
    
    # ---------------------- 好后缀规则预处理 ----------------------
    # suffix数组：suffix[k]表示长度为k的好后缀在模式串中的起始位置
    suffix = [-1] * m
    # prefix数组：prefix[k]表示长度为k的后缀是否同时是前缀
    prefix = [False] * m
    
    for i in range(m-1):
        j = i
        k = 0  # 后缀长度
        while j >= 0 and pattern[j] == pattern[m-1-k]:
            j -= 1
            k += 1
            suffix[k] = j + 1  # 记录后缀起始位置
        if j == -1:
            prefix[k] = True  # 是前缀
    
    # ---------------------- 匹配过程 ----------------------
    i = 0
    while i <= n - m:
        j = m - 1
        # 从右向左比较
        while j >= 0 and text[i+j] == pattern[j]:
            j -= 1
        
        if j == -1:
            # 找到匹配
            matches.append(i)
            i += 1
        else:
            # 计算坏字符规则移动距离
            bc_char = text[i+j]
            bc_pos = bad_char.get(bc_char, -1)
            d1 = j - bc_pos if bc_pos < j else 1
            
            # 计算好后缀规则移动距离
            d2 = 0
            if j < m - 1:  # 存在好后缀
                k = m - 1 - j  # 好后缀长度
                if suffix[k] != -1:
                    d2 = j - suffix[k] + 1
                else:
                    # 查找最长的既是后缀也是前缀的子串
                    for r in range(1, k):
                        if prefix[r]:
                            d2 = m - r
                    if d2 == 0:
                        d2 = m
            
            # 选择最大移动距离
            i += max(d1, d2)
    
    return matches
```

### 优化实现（带调试输出）
```python
def boyer_moore_optimized(text, pattern, debug=False):
    """
    Boyer-Moore算法优化实现（含调试输出）
    :param text: 主串
    :param pattern: 模式串
    :param debug: 是否输出调试信息
    :return: 匹配起始位置列表
    """
    n = len(text)
    m = len(pattern)
    matches = []
    
    if m == 0 or n < m:
        return matches
    
    # 预处理坏字符表
    bad_char = {}
    for i, c in enumerate(pattern):
        bad_char[c] = i
    
    # 预处理好后缀表
    suffix = [-1] * (m + 1)
    prefix = [False] * (m + 1)
    
    for i in range(m-1):
        j = i
        k = 0
        while j >= 0 and pattern[j] == pattern[m-1-k]:
            j -= 1
            k += 1
            suffix[k] = j + 1
        if j == -1:
            prefix[k] = True
    
    i = 0
    step = 0
    
    while i <= n - m:
        step += 1
        if debug:
            print(f"\n第{step}步: i={i}")
            print("  " + text)
            print("  " + " "*i + pattern)
            
        j = m - 1
        while j >= 0 and text[i+j] == pattern[j]:
            j -= 1
            if debug and j >= 0:
                print(f"  匹配位置: i+j={i+j}, j={j}")
        
        if j == -1:
            if debug:
                print(f"  找到匹配: 位置{i}")
            matches.append(i)
            i += 1
        else:
            # 计算坏字符距离
            bc_char = text[i+j]
            bc_pos = bad_char.get(bc_char, -1)
            d1 = j - bc_pos if bc_pos < j else 1
            
            # 计算好后缀距离
            d2 = 0
            if j < m - 1:
                k = m - 1 - j
                if suffix[k] != -1:
                    d2 = j - suffix[k] + 1
                else:
                    for r in range(1, k):
                        if prefix[r]:
                            d2 = m - r
                            break
                    if d2 == 0:
                        d2 = m
            
            move = max(d1, d2)
            if debug:
                print(f"  不匹配位置: i+j={i+j}, j={j}")
                print(f"  坏字符: {bc_char}, 位置: {bc_pos}, d1={d1}")
                print(f"  好后缀长度: {k if j < m-1 else 0}, d2={d2}")
                print(f"  移动距离: {move}")
            i += move
    
    return matches
```

---

## 📊 复杂度分析

### 时间复杂度
- **预处理阶段**: O(m + k)，其中m为模式串长度，k为字符集大小
- **匹配阶段**: 
  - 最佳情况: O(n/m)，当模式串与主串差异较大时
  - 平均情况: O(n/m)，实际应用中表现优异
  - 最坏情况: O(nm)，当所有字符都相同时
- **总体复杂度**: 平均O(n/m)，最坏O(nm)

### 空间复杂度
- **空间复杂度**: O(k + m)，主要用于存储坏字符表和好后缀数组
- **其中**: O(k)用于坏字符表，O(m)用于好后缀数组

### 复杂度对比表
| 操作 | 时间复杂度 | 空间复杂度 | 说明 |
|------|------------|------------|------|
| 坏字符表构建 | O(m) | O(k) | k为字符集大小 |
| 好后缀数组构建 | O(m²) | O(m) | 最坏情况 |
| 匹配（最佳） | O(n/m) | O(1) | 每次比较跳过多个字符 |
| 匹配（最坏） | O(nm) | O(1) | 所有字符都相同 |
| 总体（平均） | O(n/m) | O(k + m) | 实际应用中效率最高 |

---

## 🔍 应用场景

1. **文本编辑器**：高效的查找替换功能（如VS Code、Sublime Text）
2. **搜索引擎**：大规模文本索引和关键词匹配
3. **生物信息学**：DNA序列分析和基因匹配
4. **反病毒软件**：病毒特征码扫描
5. **拼写检查器**：快速定位拼写错误
6. **数据压缩**：重复字符串检测
7. **网络安全**：入侵检测系统中的特征匹配
8. **自然语言处理**：关键词提取和文本分类

---

## ⚠️ 注意事项

1. **实现复杂度**：完整实现同时包含坏字符和好后缀规则较为复杂
2. **预处理开销**：好后缀规则的预处理需要O(m²)时间和空间
3. **最坏情况性能**：当主串和模式串所有字符都相同时，退化为O(nm)
4. **字符集大小**：坏字符表大小取决于字符集，Unicode可能占用较大空间
5. **模式串长度**：对于极短模式串（m<3），简单算法可能更高效
6. **多模式匹配**：基本Boyer-Moore算法不支持多模式匹配
7. **预处理优化**：实际应用中可只实现坏字符规则以简化代码
8. **边界条件**：需特别处理模式串为空或主串短于模式串的情况

---

## 🎓 最佳实践

1. **规则选择**：
   - 简单应用可只实现坏字符规则
   - 关键应用建议同时实现两种规则以获得最佳性能
2. **预处理优化**：
   - 对好后缀规则使用空间优化版本
   - 字符集较大时使用哈希表存储坏字符位置
3. **性能调优**：
   - 对长模式串（m>100）特别有效
   - 结合启发式方法处理最坏情况
4. **实际应用**：
   - 优先使用现有库实现（如Python的re模块）
   - 自定义实现时考虑使用位运算优化比较
5. **算法选择指南**：
   - 单模式匹配且模式串较长：Boyer-Moore
   - 多模式匹配：Aho-Corasick
   - 在线匹配（流数据）：KMP
   - 硬件受限环境：简化版Boyer-Moore

---

## 📚 扩展阅读
- [维基百科：Boyer-Moore算法](https://en.wikipedia.org/wiki/Boyer%E2%80%93Moore_string-search_algorithm)
- 《算法导论》第32章：字符串匹配
- [Boyer-Moore算法原始论文](https://www.cs.utexas.edu/users/moore/publications/fstrpos.pdf)
- [Boyer-Moore算法优化版](https://www.geeksforgeeks.org/boyer-moore-algorithm-for-pattern-searching/)
- [实用字符串匹配算法比较](https://arxiv.org/pdf/1509.01626.pdf)