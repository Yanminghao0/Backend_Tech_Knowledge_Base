# ArrayDeque源码解析

> 深入分析JDK双端队列实现原理，掌握循环数组的核心机制

---

## 📚 目录

1. [整体架构](#1-整体架构)
2. [核心属性](#2-核心属性)
3. [循环数组原理](#3-循环数组原理)
4. [核心方法源码](#4-核心方法源码)
5. [扩容机制](#5-扩容机制)
6. [栈和队列操作](#6-栈和队列操作)
7. [流程图解](#7-流程图解)
8. [面试要点](#8-面试要点)

---

## 1. 整体架构

### 1.1 类继承结构

```
ArrayDeque<E>
├── extends AbstractCollection<E>
│   └── implements Collection<E>
├── implements Deque<E>
│   └── extends Queue<E>
├── implements Cloneable
└── implements Serializable
```

### 1.2 核心设计思想

```java
/**
 * ArrayDeque：基于循环数组的双端队列
 * 
 * 核心特性：
 * 1. 基于循环数组实现
 * 2. 支持两端插入和删除
 * 3. 可作为栈（LIFO）或队列（FIFO）使用
 * 4. 不允许null元素
 * 5. 非线程安全
 * 6. 比LinkedList更高效（无节点开销）
 */
public class ArrayDeque<E> extends AbstractCollection<E>
        implements Deque<E>, Cloneable, Serializable {
    
    // 存储元素的数组
    transient Object[] elements;
    
    // 头指针：指向第一个元素
    transient int head;
    
    // 尾指针：指向下一个要插入的位置
    transient int tail;
    
    // 最小初始容量
    private static final int MIN_INITIAL_CAPACITY = 8;
}
```


---

## 2. 核心属性

### 2.1 属性详解

```java
public class ArrayDeque<E> extends AbstractCollection<E>
        implements Deque<E>, Cloneable, Serializable {
    
    /**
     * 存储元素的数组
     * - 容量总是2的幂次方
     * - 便于使用位运算计算索引
     */
    transient Object[] elements;
    
    /**
     * 头指针
     * - 指向队列的第一个元素
     * - 初始值为0
     */
    transient int head;
    
    /**
     * 尾指针
     * - 指向下一个要插入的位置（尾部后一位）
     * - 初始值为0
     * - 当head == tail时，队列为空或满
     */
    transient int tail;
    
    /**
     * 最小初始容量
     */
    private static final int MIN_INITIAL_CAPACITY = 8;
}
```

### 2.2 构造方法

```java
/**
 * 默认构造：容量16
 */
public ArrayDeque() {
    elements = new Object[16];
}

/**
 * 指定初始容量
 * 实际容量会调整为大于numElements的最小2的幂次方
 */
public ArrayDeque(int numElements) {
    allocateElements(numElements);
}

/**
 * 从集合构造
 */
public ArrayDeque(Collection<? extends E> c) {
    allocateElements(c.size());
    addAll(c);
}

/**
 * 分配数组空间
 * 容量必须是2的幂次方
 */
private void allocateElements(int numElements) {
    int initialCapacity = MIN_INITIAL_CAPACITY;
    // 找到大于numElements的最小2的幂次方
    if (numElements >= initialCapacity) {
        initialCapacity = numElements;
        initialCapacity |= (initialCapacity >>>  1);
        initialCapacity |= (initialCapacity >>>  2);
        initialCapacity |= (initialCapacity >>>  4);
        initialCapacity |= (initialCapacity >>>  8);
        initialCapacity |= (initialCapacity >>> 16);
        initialCapacity++;
        
        if (initialCapacity < 0)  // 溢出
            initialCapacity >>>= 1;
    }
    elements = new Object[initialCapacity];
}
```

---

## 3. 循环数组原理

### 3.1 循环数组结构

```
循环数组示意图：

物理数组：
┌───┬───┬───┬───┬───┬───┬───┬───┐
│ 0 │ 1 │ 2 │ 3 │ 4 │ 5 │ 6 │ 7 │  索引
└───┴───┴───┴───┴───┴───┴───┴───┘

逻辑视图（循环）：
        ┌───────────────────────┐
        │                       │
        ▼                       │
┌───┬───┬───┬───┬───┬───┬───┬───┤
│ 0 │ 1 │ 2 │ 3 │ 4 │ 5 │ 6 │ 7 │
└───┴───┴───┴───┴───┴───┴───┴───┘
        │                       ▲
        │                       │
        └───────────────────────┘

示例状态：
┌───┬───┬───┬───┬───┬───┬───┬───┐
│   │   │ A │ B │ C │ D │   │   │
└───┴───┴───┴───┴───┴───┴───┴───┘
          ↑               ↑
        head=2          tail=6

元素顺序：A → B → C → D
```

### 3.2 索引计算

```java
/**
 * 循环数组的索引计算
 * 
 * 关键：使用位运算代替取模
 * (index) & (length - 1) 等价于 index % length
 * 前提：length必须是2的幂次方
 */
public class IndexCalculation {
    
    // 数组长度（必须是2的幂次方）
    int length = 8;  // 二进制：1000
    int mask = length - 1;  // 二进制：0111
    
    // 计算下一个索引
    int nextIndex(int i) {
        return (i + 1) & mask;
    }
    
    // 计算上一个索引
    int prevIndex(int i) {
        return (i - 1) & mask;
    }
    
    // 示例
    void demo() {
        // 正常情况
        int next = (5 + 1) & 7;  // 6
        
        // 循环情况
        int wrap = (7 + 1) & 7;  // 0（从7循环到0）
        
        // 负数情况
        int prev = (0 - 1) & 7;  // 7（从0循环到7）
        // -1的二进制是全1，& 0111 = 0111 = 7
    }
}
```

### 3.3 为什么容量必须是2的幂次方

```
原因：使用位运算代替取模，提高性能

取模运算：index % length
位运算：  index & (length - 1)

当length = 8时：
- 8的二进制：1000
- 7的二进制：0111

任何数 & 0111 的结果范围是 0-7，正好是数组索引范围

示例：
9 % 8 = 1
9 & 7 = 1001 & 0111 = 0001 = 1  ✓

-1 % 8 = -1（Java中负数取模结果为负）
-1 & 7 = 1111...1111 & 0111 = 0111 = 7  ✓（正确处理负数）
```

---

## 4. 核心方法源码

### 4.1 addFirst - 头部插入

```java
/**
 * 在头部插入元素
 */
public void addFirst(E e) {
    if (e == null)
        throw new NullPointerException();
    // 计算新的head位置（向前移动）
    elements[head = (head - 1) & (elements.length - 1)] = e;
    // 检查是否需要扩容
    if (head == tail)
        doubleCapacity();
}
```

### 4.2 addLast - 尾部插入

```java
/**
 * 在尾部插入元素
 */
public void addLast(E e) {
    if (e == null)
        throw new NullPointerException();
    // 在tail位置插入
    elements[tail] = e;
    // 计算新的tail位置（向后移动）
    if ((tail = (tail + 1) & (elements.length - 1)) == head)
        doubleCapacity();
}
```

### 4.3 pollFirst - 头部移除

```java
/**
 * 移除并返回头部元素
 */
public E pollFirst() {
    int h = head;
    @SuppressWarnings("unchecked")
    E result = (E) elements[h];
    if (result == null)
        return null;  // 队列为空
    elements[h] = null;  // 帮助GC
    head = (h + 1) & (elements.length - 1);  // head向后移动
    return result;
}
```

### 4.4 pollLast - 尾部移除

```java
/**
 * 移除并返回尾部元素
 */
public E pollLast() {
    int t = (tail - 1) & (elements.length - 1);  // tail向前移动
    @SuppressWarnings("unchecked")
    E result = (E) elements[t];
    if (result == null)
        return null;  // 队列为空
    elements[t] = null;  // 帮助GC
    tail = t;
    return result;
}
```

### 4.5 peekFirst/peekLast - 查看元素

```java
/**
 * 查看头部元素
 */
@SuppressWarnings("unchecked")
public E peekFirst() {
    return (E) elements[head];
}

/**
 * 查看尾部元素
 */
@SuppressWarnings("unchecked")
public E peekLast() {
    return (E) elements[(tail - 1) & (elements.length - 1)];
}
```

### 4.6 操作图解

```
addFirst(X) 操作：

Before:
┌───┬───┬───┬───┬───┬───┬───┬───┐
│   │   │ A │ B │ C │   │   │   │
└───┴───┴───┴───┴───┴───┴───┴───┘
          ↑           ↑
        head=2      tail=5

After:
┌───┬───┬───┬───┬───┬───┬───┬───┐
│   │ X │ A │ B │ C │   │   │   │
└───┴───┴───┴───┴───┴───┴───┴───┘
      ↑               ↑
    head=1          tail=5

---

addLast(Y) 操作：

Before:
┌───┬───┬───┬───┬───┬───┬───┬───┐
│   │ X │ A │ B │ C │   │   │   │
└───┴───┴───┴───┴───┴───┴───┴───┘
      ↑               ↑
    head=1          tail=5

After:
┌───┬───┬───┬───┬───┬───┬───┬───┐
│   │ X │ A │ B │ C │ Y │   │   │
└───┴───┴───┴───┴───┴───┴───┴───┘
      ↑                   ↑
    head=1              tail=6

---

pollFirst() 操作：

Before:
┌───┬───┬───┬───┬───┬───┬───┬───┐
│   │ X │ A │ B │ C │ Y │   │   │
└───┴───┴───┴───┴───┴───┴───┴───┘
      ↑                   ↑
    head=1              tail=6

After (返回X):
┌───┬───┬───┬───┬───┬───┬───┬───┐
│   │   │ A │ B │ C │ Y │   │   │
└───┴───┴───┴───┴───┴───┴───┴───┘
          ↑               ↑
        head=2          tail=6
```


---

## 5. 扩容机制

### 5.1 doubleCapacity方法

```java
/**
 * 扩容为原来的2倍
 * 触发条件：head == tail（数组满）
 */
private void doubleCapacity() {
    assert head == tail;
    int p = head;
    int n = elements.length;
    int r = n - p;  // head右边的元素数量
    int newCapacity = n << 1;  // 容量翻倍
    if (newCapacity < 0)
        throw new IllegalStateException("Sorry, deque too big");
    Object[] a = new Object[newCapacity];
    // 复制head右边的元素到新数组开头
    System.arraycopy(elements, p, a, 0, r);
    // 复制head左边的元素到新数组
    System.arraycopy(elements, 0, a, r, p);
    elements = a;
    head = 0;
    tail = n;
}
```

### 5.2 扩容过程图解

```
扩容前（容量8，已满）：
┌───┬───┬───┬───┬───┬───┬───┬───┐
│ E │ F │ G │ H │ A │ B │ C │ D │
└───┴───┴───┴───┴───┴───┴───┴───┘
                  ↑
              head=4, tail=4

逻辑顺序：A → B → C → D → E → F → G → H

扩容过程：
1. 创建新数组（容量16）
2. 复制head右边的元素（A,B,C,D）到新数组开头
3. 复制head左边的元素（E,F,G,H）到新数组

扩容后（容量16）：
┌───┬───┬───┬───┬───┬───┬───┬───┬───┬───┬───┬───┬───┬───┬───┬───┐
│ A │ B │ C │ D │ E │ F │ G │ H │   │   │   │   │   │   │   │   │
└───┴───┴───┴───┴───┴───┴───┴───┴───┴───┴───┴───┴───┴───┴───┴───┘
  ↑                               ↑
head=0                          tail=8

逻辑顺序不变：A → B → C → D → E → F → G → H
```

---

## 6. 栈和队列操作

### 6.1 作为栈使用（LIFO）

```java
/**
 * ArrayDeque作为栈使用
 * 推荐使用addFirst/removeFirst或push/pop
 */
public class StackOperations {
    
    // push - 入栈（等同于addFirst）
    public void push(E e) {
        addFirst(e);
    }
    
    // pop - 出栈（等同于removeFirst）
    public E pop() {
        return removeFirst();
    }
    
    // peek - 查看栈顶（等同于peekFirst）
    public E peek() {
        return peekFirst();
    }
}

// 使用示例
ArrayDeque<Integer> stack = new ArrayDeque<>();
stack.push(1);  // 栈：[1]
stack.push(2);  // 栈：[2, 1]
stack.push(3);  // 栈：[3, 2, 1]
stack.pop();    // 返回3，栈：[2, 1]
stack.peek();   // 返回2，栈不变
```

### 6.2 作为队列使用（FIFO）

```java
/**
 * ArrayDeque作为队列使用
 * 推荐使用addLast/removeFirst或offer/poll
 */
public class QueueOperations {
    
    // offer - 入队（等同于addLast）
    public boolean offer(E e) {
        return offerLast(e);
    }
    
    // poll - 出队（等同于pollFirst）
    public E poll() {
        return pollFirst();
    }
    
    // peek - 查看队首（等同于peekFirst）
    public E peek() {
        return peekFirst();
    }
}

// 使用示例
ArrayDeque<Integer> queue = new ArrayDeque<>();
queue.offer(1);  // 队列：[1]
queue.offer(2);  // 队列：[1, 2]
queue.offer(3);  // 队列：[1, 2, 3]
queue.poll();    // 返回1，队列：[2, 3]
queue.peek();    // 返回2，队列不变
```

### 6.3 Deque接口方法对照

```
┌────────────────────────────────────────────────────────────┐
│                  Deque 方法对照表                          │
├──────────────┬──────────────┬──────────────┬──────────────┤
│    操作      │   头部操作   │   尾部操作   │   说明       │
├──────────────┼──────────────┼──────────────┼──────────────┤
│  插入       │ addFirst(e)  │ addLast(e)   │ 失败抛异常   │
│  (Insert)   │ offerFirst(e)│ offerLast(e) │ 失败返回false│
├──────────────┼──────────────┼──────────────┼──────────────┤
│  移除       │ removeFirst()│ removeLast() │ 失败抛异常   │
│  (Remove)   │ pollFirst()  │ pollLast()   │ 失败返回null │
├──────────────┼──────────────┼──────────────┼──────────────┤
│  检查       │ getFirst()   │ getLast()    │ 失败抛异常   │
│  (Examine)  │ peekFirst()  │ peekLast()   │ 失败返回null │
└──────────────┴──────────────┴──────────────┴──────────────┘

栈操作映射：
┌──────────────┬──────────────┐
│   栈方法     │   等价方法   │
├──────────────┼──────────────┤
│   push(e)    │ addFirst(e)  │
│   pop()      │ removeFirst()│
│   peek()     │ peekFirst()  │
└──────────────┴──────────────┘

队列操作映射：
┌──────────────┬──────────────┐
│  队列方法    │   等价方法   │
├──────────────┼──────────────┤
│   offer(e)   │ offerLast(e) │
│   poll()     │ pollFirst()  │
│   peek()     │ peekFirst()  │
└──────────────┴──────────────┘
```

---

## 7. 流程图解

### 7.1 addFirst流程

```
┌─────────────────────────────────────────────────────────────┐
│                  addFirst(e) 操作流程                        │
└─────────────────────────────────────────────────────────────┘
                           │
                           ▼
                    ┌──────────────┐
                    │  e == null?  │
                    └──────────────┘
                           │
              ┌────────────┴────────────┐
              │ Yes                     │ No
              ▼                         ▼
    ┌──────────────────┐      ┌──────────────────┐
    │ 抛出NPE          │      │ head = (head-1)  │
    └──────────────────┘      │ & (length-1)     │
                              └──────────────────┘
                                       │
                                       ▼
                              ┌──────────────────┐
                              │ elements[head]=e │
                              └──────────────────┘
                                       │
                                       ▼
                              ┌──────────────────┐
                              │ head == tail?    │
                              └──────────────────┘
                                       │
                              ┌────────┴────────┐
                              │ Yes             │ No
                              ▼                 ▼
                       ┌──────────────┐  ┌──────────────┐
                       │doubleCapacity│  │    完成      │
                       └──────────────┘  └──────────────┘
```

### 7.2 ArrayDeque vs LinkedList

```
┌────────────────────────────────────────────────────────────┐
│              ArrayDeque vs LinkedList 对比                  │
├──────────────┬─────────────────┬───────────────────────────┤
│    特性      │   ArrayDeque    │      LinkedList           │
├──────────────┼─────────────────┼───────────────────────────┤
│  底层结构    │  循环数组       │  双向链表                 │
│  内存占用    │  连续内存       │  分散内存+节点开销        │
│  缓存友好    │  是             │  否                       │
│  随机访问    │  不支持         │  不支持                   │
│  两端操作    │  O(1)           │  O(1)                     │
│  中间插入    │  O(n)           │  O(1)（已定位）           │
│  null元素    │  不允许         │  允许                     │
│  扩容        │  需要           │  不需要                   │
│  GC压力      │  小             │  大（频繁创建节点）       │
│  推荐场景    │  栈/队列        │  频繁中间插入             │
└──────────────┴─────────────────┴───────────────────────────┘

性能对比（两端操作）：
ArrayDeque更快，原因：
1. 数组连续内存，缓存命中率高
2. 无节点对象创建开销
3. 无指针维护开销
```

---

## 8. 面试要点

### 8.1 高频面试题

```
Q1: ArrayDeque的实现原理？
A: 基于循环数组实现的双端队列：
   - 使用数组存储元素
   - head指向第一个元素，tail指向下一个插入位置
   - 容量必须是2的幂次方，便于位运算
   - 当head==tail时触发扩容

Q2: 为什么ArrayDeque比LinkedList快？
A: - 数组连续内存，缓存命中率高
   - 无节点对象创建开销
   - 无指针维护开销
   - 位运算计算索引，效率高

Q3: ArrayDeque是否允许null？
A: 不允许。因为null被用作判断队列是否为空的标志。

Q4: ArrayDeque是否线程安全？
A: 不是。多线程环境下应使用ConcurrentLinkedDeque或加锁。

Q5: ArrayDeque作为栈和队列时应该用哪些方法？
A: - 栈：push/pop/peek（或addFirst/removeFirst/peekFirst）
   - 队列：offer/poll/peek（或addLast/pollFirst/peekFirst）
```

### 8.2 使用示例

```java
/**
 * ArrayDeque 使用示例
 */
public class ArrayDequeDemo {
    
    public static void main(String[] args) {
        // 1. 作为栈使用
        Deque<Integer> stack = new ArrayDeque<>();
        stack.push(1);
        stack.push(2);
        stack.push(3);
        System.out.println(stack.pop());  // 3
        System.out.println(stack.peek()); // 2
        
        // 2. 作为队列使用
        Queue<Integer> queue = new ArrayDeque<>();
        queue.offer(1);
        queue.offer(2);
        queue.offer(3);
        System.out.println(queue.poll()); // 1
        System.out.println(queue.peek()); // 2
        
        // 3. 作为双端队列使用
        Deque<Integer> deque = new ArrayDeque<>();
        deque.addFirst(1);  // [1]
        deque.addLast(2);   // [1, 2]
        deque.addFirst(0);  // [0, 1, 2]
        deque.addLast(3);   // [0, 1, 2, 3]
        System.out.println(deque.pollFirst()); // 0
        System.out.println(deque.pollLast());  // 3
    }
}
```

### 8.3 典型应用场景

```java
/**
 * ArrayDeque 典型应用
 */
public class ArrayDequeApplications {
    
    // 1. 括号匹配
    public boolean isValid(String s) {
        Deque<Character> stack = new ArrayDeque<>();
        for (char c : s.toCharArray()) {
            if (c == '(' || c == '[' || c == '{') {
                stack.push(c);
            } else {
                if (stack.isEmpty()) return false;
                char top = stack.pop();
                if (c == ')' && top != '(') return false;
                if (c == ']' && top != '[') return false;
                if (c == '}' && top != '{') return false;
            }
        }
        return stack.isEmpty();
    }
    
    // 2. 滑动窗口最大值
    public int[] maxSlidingWindow(int[] nums, int k) {
        if (nums.length == 0) return new int[0];
        int[] result = new int[nums.length - k + 1];
        Deque<Integer> deque = new ArrayDeque<>();  // 存储索引
        
        for (int i = 0; i < nums.length; i++) {
            // 移除窗口外的元素
            while (!deque.isEmpty() && deque.peekFirst() < i - k + 1) {
                deque.pollFirst();
            }
            // 移除比当前元素小的元素
            while (!deque.isEmpty() && nums[deque.peekLast()] < nums[i]) {
                deque.pollLast();
            }
            deque.offerLast(i);
            // 记录窗口最大值
            if (i >= k - 1) {
                result[i - k + 1] = nums[deque.peekFirst()];
            }
        }
        return result;
    }
    
    // 3. BFS层序遍历
    public List<List<Integer>> levelOrder(TreeNode root) {
        List<List<Integer>> result = new ArrayList<>();
        if (root == null) return result;
        
        Queue<TreeNode> queue = new ArrayDeque<>();
        queue.offer(root);
        
        while (!queue.isEmpty()) {
            int size = queue.size();
            List<Integer> level = new ArrayList<>();
            for (int i = 0; i < size; i++) {
                TreeNode node = queue.poll();
                level.add(node.val);
                if (node.left != null) queue.offer(node.left);
                if (node.right != null) queue.offer(node.right);
            }
            result.add(level);
        }
        return result;
    }
    
    static class TreeNode {
        int val;
        TreeNode left, right;
    }
}
```

### 8.4 注意事项

```java
/**
 * 使用ArrayDeque的注意事项
 */
public class ArrayDequeNotes {
    
    // 1. 不允许null
    // ❌ 错误
    // ArrayDeque<String> deque = new ArrayDeque<>();
    // deque.add(null);  // NullPointerException
    
    // 2. 非线程安全
    // ❌ 多线程直接使用
    // ✅ 使用ConcurrentLinkedDeque或加锁
    
    // 3. 推荐替代Stack类
    // ❌ 不推荐
    // Stack<Integer> stack = new Stack<>();
    // ✅ 推荐
    Deque<Integer> stack = new ArrayDeque<>();
    
    // 4. 推荐替代LinkedList作为队列
    // ❌ 性能较差
    // Queue<Integer> queue = new LinkedList<>();
    // ✅ 性能更好
    Queue<Integer> queue = new ArrayDeque<>();
}
```

---

## 📚 总结

### 核心要点

1. **数据结构**：基于循环数组实现
2. **容量要求**：必须是2的幂次方
3. **索引计算**：使用位运算，效率高
4. **扩容策略**：容量翻倍
5. **null限制**：不允许null元素

### 适用场景

- 栈（替代Stack类）
- 队列（替代LinkedList）
- 双端队列
- BFS/DFS算法

### 性能优势

- 比LinkedList更快（缓存友好）
- 比Stack更快（非同步）
- 内存占用更少（无节点开销）

---

*最后更新：2025-12-28*
