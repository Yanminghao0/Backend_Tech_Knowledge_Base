# 缓存问题排查

> 实战指南：如何排查Redis连接、缓存穿透、击穿、雪崩等缓存问题

## 📋 目录
- [常见问题类型](#常见问题类型)
- [Redis连接问题](#redis连接问题)
- [缓存穿透](#缓存穿透)
- [缓存击穿](#缓存击穿)
- [缓存雪崩](#缓存雪崩)
- [大key和热key](#大key和热key)

---

## 常见问题类型

### 1. Redis连接问题

**症状**：
```
✅ 连接超时
✅ 连接数达到上限
✅ 连接池满
✅ 连接异常
```

### 2. 缓存穿透

**症状**：
```
✅ 大量请求打到数据库
✅ 缓存命中率低
✅ 数据库压力大
```

### 3. 缓存击穿

**症状**：
```
✅ 热点key过期
✅ 大量请求打到数据库
✅ 数据库压力突增
```

### 4. 缓存雪崩

**症状**：
```
✅ 大量key同时过期
✅ 数据库压力突增
✅ 系统性能下降
```

---

## Redis连接问题

### 排查方法

**1. 测试Redis连接**

```bash
# 测试连接
redis-cli -h <host> -p <port> PING

# 查看连接信息
redis-cli INFO server
```

**2. 查看连接数**

```bash
# 查看当前连接数
redis-cli CLIENT LIST | wc -l

# 查看连接详情
redis-cli CLIENT LIST

# 查看最大连接数
redis-cli CONFIG GET maxclients
```

**3. 查看连接池状态**

```java
// Jedis连接池监控
JedisPool pool = jedisPool;
JedisPoolConfig config = pool.getPoolConfig();
System.out.println("Active: " + pool.getNumActive());
System.out.println("Idle: " + pool.getNumIdle());
System.out.println("Waiters: " + pool.getNumWaiters());
```

### 常见原因

**1. 连接数达到上限**

```bash
# 查看最大连接数
redis-cli CONFIG GET maxclients

# 增加最大连接数
redis-cli CONFIG SET maxclients 10000
```

**2. 连接泄漏**

```java
// ❌ 连接未关闭
Jedis jedis = jedisPool.getResource();
jedis.set("key", "value");
// 忘记关闭连接

// ✅ 使用try-with-resources
try (Jedis jedis = jedisPool.getResource()) {
    jedis.set("key", "value");
}
```

**3. 连接池配置过小**

```yaml
# application.yml
spring:
  redis:
    lettuce:
      pool:
        max-active: 10  # 太小
        max-idle: 5
        min-idle: 2
```

### 解决方案

**1. 增加连接数限制**

```bash
# 临时增加
redis-cli CONFIG SET maxclients 10000

# 永久生效（修改配置文件）
maxclients 10000
```

**2. 检查连接泄漏**

```java
// 启用连接泄漏检测
// 监控连接池状态
// 及时关闭连接
```

**3. 优化连接池配置**

```yaml
spring:
  redis:
    lettuce:
      pool:
        max-active: 20
        max-idle: 10
        min-idle: 5
        max-wait: 1000
```

---

## 缓存穿透

### 问题描述

**缓存穿透**：查询不存在的数据，缓存未命中，请求直接打到数据库

**影响**：
```
- 大量请求打到数据库
- 数据库压力大
- 可能影响正常业务
```

### 排查方法

**1. 查看缓存命中率**

```bash
# 查看命中率
redis-cli INFO stats | grep keyspace_hits
redis-cli INFO stats | grep keyspace_misses

# 计算命中率
命中率 = keyspace_hits / (keyspace_hits + keyspace_misses)
```

**2. 分析请求日志**

```bash
# 查看请求日志
grep "cache miss" application.log
grep "database query" application.log
```

### 解决方案

**1. 布隆过滤器**

```java
// 使用布隆过滤器快速判断
if (!bloomFilter.exists(key)) {
    return null;  // 不存在，直接返回
}

// 查询缓存
String value = redis.get(key);
if (value != null) {
    return value;
}

// 查询数据库
value = db.query(key);
if (value != null) {
    redis.set(key, value);
} else {
    // 缓存空值（短期）
    redis.setex(key, 300, "");  // 5分钟
}
```

**2. 缓存空值**

```java
// 查询不存在的数据，缓存空值
String value = redis.get(key);
if (value != null) {
    if (value.isEmpty()) {
        return null;  // 空值，直接返回
    }
    return value;
}

value = db.query(key);
if (value != null) {
    redis.set(key, value);
} else {
    // 缓存空值（短期，避免穿透）
    redis.setex(key, 300, "");  // 5分钟
}
```

**3. 参数校验**

```java
// 参数校验，过滤无效请求
if (id == null || id <= 0) {
    return null;
}
```

---

## 缓存击穿

### 问题描述

**缓存击穿**：热点key过期，大量请求同时打到数据库

**影响**：
```
- 数据库压力突增
- 可能影响系统性能
```

### 排查方法

**1. 查看热点key**

```bash
# 查看热key
redis-cli --hotkeys

# 分析key访问频率
redis-cli MONITOR | grep "GET"
```

**2. 分析请求模式**

```bash
# 查看请求日志
grep "cache miss" application.log
grep "database query" application.log
```

### 解决方案

**1. 热点key永不过期**

```java
// 热点key设置为永不过期
redis.set("hot:key", value);
redis.persist("hot:key");  // 取消过期时间
```

**2. 互斥锁更新缓存**

```java
// 使用分布式锁，只允许一个线程更新缓存
String lockKey = "lock:" + key;
if (redis.setnx(lockKey, "1", 60)) {  // 获取锁
    try {
        // 查询数据库
        String value = db.query(key);
        if (value != null) {
            redis.set(key, value);
        }
    } finally {
        redis.del(lockKey);  // 释放锁
    }
} else {
    // 未获取到锁，等待一段时间后重试
    Thread.sleep(100);
    return redis.get(key);
}
```

**3. 逻辑过期（异步更新）**

```java
// 逻辑过期：key永不过期，但存储过期时间
String value = redis.get(key);
if (value != null) {
    long expireTime = getExpireTime(key);
    if (expireTime > System.currentTimeMillis()) {
        return value;  // 未过期
    }
    // 过期，异步更新
    asyncUpdateCache(key);
    return value;  // 返回旧值
}
```

---

## 缓存雪崩

### 问题描述

**缓存雪崩**：大量key同时过期，大量请求同时打到数据库

**影响**：
```
- 数据库压力突增
- 系统性能下降
- 可能影响系统稳定性
```

### 排查方法

**1. 查看key过期情况**

```bash
# 查看key过期时间
redis-cli TTL key

# 分析过期时间分布
redis-cli --scan | while read key; do
    echo "$key: $(redis-cli TTL $key)"
done
```

**2. 分析请求模式**

```bash
# 查看请求日志
grep "cache miss" application.log | tail -100
```

### 解决方案

**1. 过期时间加随机值**

```java
// 过期时间加随机值，避免同时过期
int expireTime = 3600 + (int)(Math.random() * 600);  // 3600-4200秒
redis.setex(key, expireTime, value);
```

**2. 多级缓存**

```java
// 多级缓存：本地缓存 + Redis
// 本地缓存：Caffeine（更短过期时间）
// Redis缓存：较长过期时间
```

**3. 限流降级**

```java
// 限流保护：限制数据库请求数
if (rateLimiter.tryAcquire()) {
    // 查询数据库
} else {
    // 返回默认值或降级
    return getDefaultValue();
}
```

**4. 缓存预热**

```java
// 系统启动时，预热热点数据
@PostConstruct
public void warmUpCache() {
    List<String> hotKeys = getHotKeys();
    for (String key : hotKeys) {
        String value = db.query(key);
        redis.set(key, value);
    }
}
```

---

## 大key和热key

### 大key问题

**症状**：
```
✅ Redis内存占用高
✅ 操作慢（GET/SET）
✅ 网络传输慢
✅ 可能阻塞其他操作
```

**排查方法**：

```bash
# 查找大key
redis-cli --bigkeys

# 分析key大小
redis-cli MEMORY USAGE key
```

**解决方案**：

```java
// 1. 拆分大key
// ❌ 大key
redis.set("user:10001:orders", largeJson);

// ✅ 拆分
redis.set("user:10001:orders:count", count);
redis.lpush("user:10001:orders", order1, order2, ...);

// 2. 压缩数据
String compressed = compress(largeJson);
redis.set("key", compressed);

// 3. 使用Hash分片
for (int i = 0; i < 100; i++) {
    redis.hset("key:hash", "field" + i, value);
}
```

### 热key问题

**症状**：
```
✅ 单个key访问频率高
✅ Redis CPU使用率高
✅ 可能成为瓶颈
```

**排查方法**：

```bash
# 查找热key
redis-cli --hotkeys

# 监控key访问
redis-cli MONITOR | grep "GET"
```

**解决方案**：

```java
// 1. 本地缓存
// 使用Caffeine本地缓存热点数据
Cache<String, String> localCache = Caffeine.newBuilder()
    .maximumSize(1000)
    .expireAfterWrite(5, TimeUnit.MINUTES)
    .build();

String value = localCache.get(key, k -> redis.get(k));

// 2. 主从读写分离
// 读请求路由到从库
Jedis jedis = slaveJedisPool.getResource();
String value = jedis.get(key);

// 3. key分片
// 将热点key分片到多个key
String shardKey = "key:" + (hash(key) % 10);
String value = redis.get(shardKey);
```

---

## 缓存优化建议

### 1. 缓存策略

```
✅ Cache Aside（旁路缓存）
✅ Read Through（读穿透）
✅ Write Through（写穿透）
✅ Write Behind（写回）
```

### 2. 缓存设计

```
✅ 合理设置过期时间
✅ 避免缓存穿透/击穿/雪崩
✅ 处理大key和热key
✅ 监控缓存命中率
```

### 3. 缓存监控

```
✅ 缓存命中率
✅ 缓存大小
✅ 连接数
✅ 慢查询
```

---

## 📚 相关文档

- [性能问题排查](./01_性能问题排查.md)
- [Redis核心机制详解](../../04_缓存/Redis核心机制详解.md)
- [缓存架构设计与实战](../../04_缓存/缓存架构设计与实战.md)

---

**最后更新**: 2025-10-29  
**文档状态**: ✅ 框架已搭建，内容持续完善中

