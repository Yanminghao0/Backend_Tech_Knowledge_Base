# Cursor 项目规则配置

## 模型输出规则
每次AI助手输出回复时，必须在**对话响应**的末尾标注当前使用的模型型号。

**重要**：模型型号信息只出现在对话返回的结果中，**不要**放在生成的文件内容中。

### 输出格式
在每次对话回复的末尾添加：
```
---
🤖 当前模型：[模型名称和版本]
```

### 示例
```markdown
---
🤖 当前模型：Claude 3.5 Sonnet
```

### 适用场景
- ✅ 对话回复消息
- ✅ 问题解答响应
- ❌ **不适用**：生成的 md 文件内容
- ❌ **不适用**：生成的代码文件内容
- ❌ **不适用**：任何写入文件系统的文件

### 目的
- 便于追踪不同模型的输出质量
- 方便进行模型效果对比
- 有助于优化提示词设计
- 提高工作透明度

---

## 其他项目规则
<!-- 可以在这里添加其他项目相关的规则 -->
